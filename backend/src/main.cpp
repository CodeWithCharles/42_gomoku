#include <cstdio>

#include "gomoku/types.hpp"

// temp entry point
int main() {
	std::printf("Gomoku: engine not implemented yet (%dx%d board, %d cells)\n", gomoku::kBoardSize,
				gomoku::kBoardSize, gomoku::kCellCount);
	return 0;
}
